## Packages
framer-motion | Page transitions and scroll-triggered animations
embla-carousel-react | For the testimonials carousel (already in stack but confirming need)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  sans: ["var(--font-sans)"],
}
We are forcing a dark theme natively in index.css to match the "Dark theme with black, red, and white color palette" requirement perfectly without relying on user toggles.
